<DefaultWindow xmlns="https://spacestation14.io"
               xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
               MinSize="380 480"
               SetSize="420 560"
               Title="{Loc 'union-clipboard-menu-title'}">
    <PanelContainer HorizontalExpand="True" VerticalExpand="True">
        <PanelContainer.PanelOverride>
            <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
        </PanelContainer.PanelOverride>
        <ScrollContainer Name="MemberListScroll"
                         HScrollEnabled="False"
                         HorizontalExpand="True"
                         VerticalExpand="True">
            <BoxContainer Name="MemberList"
                          Orientation="Vertical"
                          HorizontalExpand="True"
                          Margin="4 4 4 4" />
        </ScrollContainer>
        <BoxContainer Name="LockedContainer"
                      Orientation="Vertical"
                      HorizontalExpand="True"
                      VerticalExpand="True"
                      Margin="8 8 8 8"
                      Visible="False">
            <RichTextLabel Name="LockedLabel" HorizontalExpand="True" />
            <Button Name="LockedCancelButton" Text="{Loc 'union-clipboard-locked-cancel'}" Margin="0 4 0 0" />
        </BoxContainer>
    </PanelContainer>
</DefaultWindow>
